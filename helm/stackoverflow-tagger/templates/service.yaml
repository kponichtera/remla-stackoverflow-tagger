apiVersion: v1
kind: Service
metadata:
  name: {{ include "stackoverflow-tagger.fullname" . }}
  labels:
    {{- include "stackoverflow-tagger.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "stackoverflow-tagger.selectorLabels" . | nindent 4 }}
